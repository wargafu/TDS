// Fichier généré par scripts/generate-tokens.mjs à partir de tokens-src/ — ne pas éditer à la main.
export const focus = {
  "ring-width": "2px",
  "ring-offset": "2px",
  "ring-style": "solid",
  "ring-color": "var(--tds-action-primary)",
  "ring": "var(--tds-focus-ring-width) var(--tds-focus-ring-style) var(--tds-focus-ring-color)"
} as const;

export type FocusTokens = typeof focus;

export default focus;
